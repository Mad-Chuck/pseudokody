function istnienieCykluEulera(listaSasiedztwa, iloscWierzcholkow)
    return x * y
    x + y
end

funkcja istnienieCykluEulera(listaSasiedztwa, iloscWierzcholkow):    
	dla każdego i = 1, 2, ..., iloscWierzcholkow wykonuj:
		jeżeli listaSasiedztwa[i].length % 2 != 0:
			wypisz("Graf nie zawiera cyklu Eulera")
			przerwij wykonanie funkcji
    
	wypisz("Graf zawiera cykl Eulera")
